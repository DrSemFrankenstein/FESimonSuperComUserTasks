// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#455A64",
    },
    secondary: {
      main: "#9E9E9E", 
    },
  },
});

export default theme;
